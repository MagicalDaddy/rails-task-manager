<h1>My tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, "#{task.id}" %>
    <%= link_to "Delete me", task_path(task),
    method: :delete, data: { confirm: "Are you sure?" } %>
  </li>
  <% end %>

</ul>
